# Copy this file to `.env` and fill in a value if you want token-based auth.
# Generate a token by running `claude setup-token` on your host machine.
#
# Leave this blank to instead log in interactively the first time you run
# the container (the login is persisted via the ./.claude-config mount).

CLAUDE_CODE_OAUTH_TOKEN=

